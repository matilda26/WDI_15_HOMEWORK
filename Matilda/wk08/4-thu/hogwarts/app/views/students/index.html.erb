<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, students_path(student.id) %> - <%= House.where(id: student.house_id)[0].name %>
<% end %>
  </div>
